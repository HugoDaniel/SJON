{
  "name": "@sjon/wasm-consumer-example",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Reference Node.js consumer of sjon.wasm + sjon-binary.wasm. Tested under `node --test`.",
  "license": "SEE ../LICENSE",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types 'test/*.test.ts' '*.test.ts'",
    "demo": "node --experimental-strip-types demo.ts",
    "build:browser": "tsc -p tsconfig.browser.json"
  },
  "dependencies": {
    "@sjon/schema": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.0",
    "typescript": "^5.8.0"
  },
  "exports": {
    ".": "./sjon-reader.ts",
    "./SjonHost": "./SjonHost.ts",
    "./SjonSchemaBackend": "./SjonSchemaBackend.ts",
    "./createNodeFsResolver": "./createNodeFsResolver.ts"
  }
}
