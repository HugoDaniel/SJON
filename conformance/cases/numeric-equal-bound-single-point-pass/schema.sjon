; Single-point range — :min == :max — accepts exactly the point.
(plugin :name numeric-equal-bound-single-point-pass :version "1.0.0"
  (value-kind :name pin
    :underlying number
    :numeric (numeric-bounds :min 5 :max 5))
  (form :name set
    (key :name v :type pin :optional false)))
