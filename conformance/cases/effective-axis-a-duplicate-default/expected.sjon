; Post-graduation (axis A on): both `(phrase)` instances materialize
; the same defaulted `origin` name and collide -> a single
; `duplicate_cross_ref_target` from `emitDuplicateCrossRef`, which
; surfaces on the duplicate form's span and carries an empty path.
(diagnostics
  (diagnostic :code duplicate_cross_ref_target :path []))
