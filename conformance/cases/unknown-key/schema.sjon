; Closed form `scene` declares only `:title`. Any other keyword on it
; trips `unknown_key`.
(plugin :name unk-key-test :version "1.0.0"
  (form :name scene
    (key :name title :type string :optional true)))
