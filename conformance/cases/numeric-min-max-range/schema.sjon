; Value inside an inclusive [min, max] range passes.
(plugin :name numeric-min-max-range :version "1.0.0"
  (value-kind :name nn
    :underlying number
    :numeric (numeric-bounds :min 0 :max 1))
  (form :name set
    (key :name v :type nn :optional false)))
