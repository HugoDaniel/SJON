; Sibling plugin — collides with schema.sjon on form `phrase`. The
; cross-ref's bare `:target phrase` can't resolve to one specific form.
(plugin :name ambig-xref-tgt-b :version "1.0.0"
  (form :name phrase
    (key :name name :type symbol :optional false)))
