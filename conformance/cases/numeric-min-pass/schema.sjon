; A bound value at or above an inclusive :min passes.
(plugin :name numeric-min-pass :version "1.0.0"
  (value-kind :name nn
    :underlying number
    :numeric (numeric-bounds :min 0))
  (form :name set
    (key :name v :type nn :optional false)))
