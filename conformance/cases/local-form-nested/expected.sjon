; Local `group` carries its own local `:child` slot (local `dot`). `nope`
; misses there → unknown_local_form at the nested slot [canvas shape group child].
(diagnostics
  (diagnostic :code unknown_local_form :path [canvas shape group child]))
