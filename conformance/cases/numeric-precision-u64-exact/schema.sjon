; A u64 literal above :max fires `number_above_max` via the exact-int path.
(plugin :name numeric-precision-u64-exact :version "1.0.0"
  (value-kind :name nn
    :underlying number
    :numeric (numeric-bounds :max 1000))
  (form :name set
    (key :name v :type nn :optional false)))
