; Same `[<plugin> <kind> cross-ref]` path and `{0,0}` span as the rest of
; the schema-aggregate cross-ref family.
(diagnostics
  (diagnostic :code cross_ref_source_key_unknown
              :path [probe uniform-name cross-ref]))
