; A string value at or above :min-len passes.
(plugin :name string-min-len-pass :version "1.0.0"
  (value-kind :name ss
    :underlying string
    :string-bounds (string-bounds :min-len 3))
  (form :name set
    (key :name v :type ss :optional false)))
