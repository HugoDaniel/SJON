; `buffer` exists only as a positional-local on `bind-group`. At top level
; there is no slot context, so it resolves against the global catalog →
; ordinary unknown_form.
(diagnostics
  (diagnostic :code unknown_form :path [buffer]))
