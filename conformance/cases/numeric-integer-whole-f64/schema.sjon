; :integer true passes whole-valued f64 (3.0 has @floor(v) == v).
(plugin :name numeric-integer-whole-f64 :version "1.0.0"
  (value-kind :name nn
    :underlying number
    :numeric (numeric-bounds :integer true))
  (form :name set
    (key :name v :type nn :optional false)))
