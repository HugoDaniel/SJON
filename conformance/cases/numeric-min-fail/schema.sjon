; Value strictly below inclusive :min trips `number_below_min`.
(plugin :name numeric-min-fail :version "1.0.0"
  (value-kind :name nn
    :underlying number
    :numeric (numeric-bounds :min 0))
  (form :name set
    (key :name v :type nn :optional false)))
