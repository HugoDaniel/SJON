// sjon-export-version: 1
// Generated by sjon — do not edit by hand.
// Describes the canonical JSON shape of `sjon to-json`.
//
// Export warnings:
//   [warn cross_ref_unenforceable] value-kind `anchor-name` cross-ref: schema validates symbol shape only; closed-set membership requires SJON-aware validator
//   [info cross_ref_annotation_only] value-kind `anchor-name` cross-ref annotation surfaces target-form=`anchor` name-key=`name` acyclic=false scope-form=``; none are enforceable by JSON Schema
//   [warn cross_ref_unenforceable] value-kind `template-name` cross-ref: schema validates symbol shape only; closed-set membership requires SJON-aware validator
//   [info cross_ref_annotation_only] value-kind `template-name` cross-ref annotation surfaces target-form=`template` name-key=`name` acyclic=false scope-form=``; none are enforceable by JSON Schema

export type Keyword<S extends string = string> = { readonly $kw: S };
export type Symbol_<S extends string = string> = { readonly $sym: S };
export type SjonDate = { readonly $date: string };
export type SjonTime = { readonly $time: string };
export type SjonExpr<TResult = unknown> = { readonly $expr: unknown[] } & { readonly __sjonResult?: TResult };
export type CrossRef<TargetForm extends string = string, S extends string = string> = Symbol_<S> & { readonly __sjonRef?: TargetForm };
export type F32 = number & { readonly __sjonRepr?: "f32" };
export type U32 = number & { readonly __sjonRepr?: "u32" };
export type I32 = number & { readonly __sjonRepr?: "i32" };
export type U16 = number & { readonly __sjonRepr?: "u16" };
export type F16 = number & { readonly __sjonRepr?: "f16" };


// ----- plugin: template -----

/** A named point an animation can target. */
export interface Template_Anchor {
  $form: "anchor";
  $ns: "template";
  name: Symbol_;
}

/** A finished animation. Every key is checked when this form is written at the top level. */
export interface Template_Animate {
  $form: "animate";
  $ns: "template";
  ease?: Symbol_<"linear"> | Symbol_<"ease-in"> | Symbol_<"ease-out">;
  from: number;
  /**
   * @sjon-cross-ref target=`anchor` name-key=`name` acyclic=false
   */
  target: CrossRef<"anchor">;
  to: number;
}

/** A named body the host expands later. `:body` is typed `form` and read by nobody here. */
export interface Template_Template {
  $form: "template";
  $ns: "template";
  /** Opaque: the slot is a form, and what is inside it is the expander's business. */
  body: { readonly $form: string; readonly $ns?: string };
  name: Symbol_;
}

/** Expand a template against a target. */
export interface Template_Apply {
  $form: "apply";
  $ns: "template";
  /**
   * @sjon-cross-ref target=`anchor` name-key=`name` acyclic=false
   */
  target: CrossRef<"anchor">;
  /**
   * @sjon-cross-ref target=`template` name-key=`name` acyclic=false
   */
  template: CrossRef<"template">;
}

export type SjonTemplate =
  | Template_Anchor
  | Template_Animate
  | Template_Template
  | Template_Apply;

