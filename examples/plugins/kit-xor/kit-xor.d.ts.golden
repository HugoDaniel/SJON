// sjon-export-version: 1
// Generated by sjon — do not edit by hand.
// Describes the canonical JSON shape of `sjon to-json`.
//
// Export warnings:
//   [info exclusive_group_unenforceable] form `phrase` exclusive groups emitted structurally (`oneOf` for exactly_one, `not:{allOf}` for at_most_one); source-order constraints between variant keys and discriminants remain SJON-only
//   [info exclusive_group_unenforceable] form `tag` exclusive groups emitted structurally (`oneOf` for exactly_one, `not:{allOf}` for at_most_one); source-order constraints between variant keys and discriminants remain SJON-only

export type Keyword<S extends string = string> = { readonly $kw: S };
export type Symbol_<S extends string = string> = { readonly $sym: S };
export type SjonDate = { readonly $date: string };
export type SjonTime = { readonly $time: string };
export type SjonExpr<TResult = unknown> = { readonly $expr: unknown[] } & { readonly __sjonResult?: TResult };
export type CrossRef<TargetForm extends string = string, S extends string = string> = Symbol_<S> & { readonly __sjonRef?: TargetForm };
export type F32 = number & { readonly __sjonRepr?: "f32" };
export type U32 = number & { readonly __sjonRepr?: "u32" };
export type I32 = number & { readonly __sjonRepr?: "i32" };
export type U16 = number & { readonly __sjonRepr?: "u16" };
export type F16 = number & { readonly __sjonRepr?: "f16" };


// ----- plugin: kit-xor -----

/**
 * A single phrase rendered either as notes or as events, never both and never neither.
 *
 * @sjon-exclusive-group exactly-one [notes, events]
 */
export interface KitXor_Phrase {
  $form: "phrase";
  $ns: "kit-xor";
  /** Time-stamped event list. */
  events?: Array<unknown>;
  name: Symbol_;
  /** Pitch-class sequence. */
  notes?: Array<unknown>;
}

/**
 * Optional decoration on a phrase. Colour and shape are mutually exclusive but both omittable.
 *
 * @sjon-exclusive-group at-most-one [color, shape]
 */
export interface KitXor_Tag {
  $form: "tag";
  $ns: "kit-xor";
  color?: string;
  name: Symbol_;
  shape?: string;
}

/** A point on a phrase. The offset and length are measured FROM :target, so neither means anything without it. */
export interface KitXor_Anchor {
  $form: "anchor";
  $ns: "kit-xor";
  /**
   * Extent from :offset.
   * @sjon-requires target
   */
  length?: number;
  name: Symbol_;
  /**
   * Distance from :target.
   * @sjon-requires target
   */
  offset?: number;
  /** The phrase this anchor is measured against. */
  target?: Symbol_;
}

export type SjonKitXor =
  | KitXor_Phrase
  | KitXor_Tag
  | KitXor_Anchor;

