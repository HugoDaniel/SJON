// sjon-export-version: 1
// Generated by sjon — do not edit by hand.
// Describes the canonical JSON shape of `sjon to-json`.
//
// Export warnings:
//   [info exclusive_group_unenforceable] form `route` exclusive groups emitted structurally (`oneOf` for exactly_one, `not:{allOf}` for at_most_one); source-order constraints between variant keys and discriminants remain SJON-only
//   [info multi_key_exclusive_emitted] form `route` has at least one multi-key bundle in an exclusive group; emitted as `{required: [<bundle>]}` per bundle. Bundle atomicity (partial bundles fail) requires SJON-aware runtime validation
//   [info exclusive_group_unenforceable] form `tag` exclusive groups emitted structurally (`oneOf` for exactly_one, `not:{allOf}` for at_most_one); source-order constraints between variant keys and discriminants remain SJON-only
//   [info multi_key_exclusive_emitted] form `tag` has at least one multi-key bundle in an exclusive group; emitted as `{required: [<bundle>]}` per bundle. Bundle atomicity (partial bundles fail) requires SJON-aware runtime validation

export type Keyword<S extends string = string> = { readonly $kw: S };
export type Symbol_<S extends string = string> = { readonly $sym: S };
export type SjonDate = { readonly $date: string };
export type SjonTime = { readonly $time: string };
export type SjonExpr<TResult = unknown> = { readonly $expr: unknown[] } & { readonly __sjonResult?: TResult };
export type CrossRef<TargetForm extends string = string, S extends string = string> = Symbol_<S> & { readonly __sjonRef?: TargetForm };
export type F32 = number & { readonly __sjonRepr?: "f32" };
export type U32 = number & { readonly __sjonRepr?: "u32" };
export type I32 = number & { readonly __sjonRepr?: "i32" };
export type U16 = number & { readonly __sjonRepr?: "u16" };
export type F16 = number & { readonly __sjonRepr?: "f16" };


// ----- plugin: xkey -----

/**
 * A route declaration. Either name both endpoints `(from + to)` or supply a single `at` waypoint.
 *
 * @sjon-exclusive-group exactly-one [from + to, at]
 */
export interface Xkey_Route {
  $form: "route";
  $ns: "xkey";
  at?: Symbol_;
  from?: Symbol_;
  name: Symbol_;
  to?: Symbol_;
}

/**
 * A tag decoration. Use `(prefix + suffix)` to flank, or `literal` alone, but never both.
 *
 * @sjon-exclusive-group at-most-one [prefix + suffix, literal]
 */
export interface Xkey_Tag {
  $form: "tag";
  $ns: "xkey";
  literal?: string;
  name: Symbol_;
  prefix?: string;
  suffix?: string;
}

export type SjonXkey =
  | Xkey_Route
  | Xkey_Tag;

