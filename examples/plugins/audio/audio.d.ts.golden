// sjon-export-version: 1
// Generated by sjon — do not edit by hand.
// Describes the canonical JSON shape of `sjon to-json`.
//
// Export warnings:
//   [info union_emitted_via_anyof] value-kind `note-or-event` union_of emitted as JSON Schema `anyOf` over resolved alternatives; SJON's first-match dispatch order is preserved in `x-sjon-union-alternatives`
//   [info head_set_emitted_via_oneof_refs] value-kind `event` head-set emitted as `oneOf` of `$ref`s into `#/$defs/form.<plugin>.<head>`
//   [info union_emitted_via_anyof] value-kind `phrase-or-ref` union_of emitted as JSON Schema `anyOf` over resolved alternatives; SJON's first-match dispatch order is preserved in `x-sjon-union-alternatives`
//   [info head_set_emitted_via_oneof_refs] value-kind `phrase-inline` head-set emitted as `oneOf` of `$ref`s into `#/$defs/form.<plugin>.<head>`
//   [warn cross_ref_unenforceable] value-kind `phrase-ref` cross-ref: schema validates symbol shape only; closed-set membership requires SJON-aware validator
//   [info cross_ref_annotation_only] value-kind `phrase-ref` cross-ref annotation surfaces target-form=`phrase` name-key=`name` acyclic=false scope-form=``; none are enforceable by JSON Schema

export type Keyword<S extends string = string> = { readonly $kw: S };
export type Symbol_<S extends string = string> = { readonly $sym: S };
export type SjonDate = { readonly $date: string };
export type SjonTime = { readonly $time: string };
export type SjonExpr<TResult = unknown> = { readonly $expr: unknown[] } & { readonly __sjonResult?: TResult };
export type CrossRef<TargetForm extends string = string, S extends string = string> = Symbol_<S> & { readonly __sjonRef?: TargetForm };
export type F32 = number & { readonly __sjonRepr?: "f32" };
export type U32 = number & { readonly __sjonRepr?: "u32" };
export type I32 = number & { readonly __sjonRepr?: "i32" };
export type U16 = number & { readonly __sjonRepr?: "u16" };
export type F16 = number & { readonly __sjonRepr?: "f16" };


// ----- plugin: audio -----

/** A note sequence, either named (a reusable definition) or anonymous (inline). */
export interface Audio_Phrase {
  $form: "phrase";
  $ns: "audio";
  name?: Symbol_;
  notes: Array<Symbol_<"E4"> | Symbol_<"G4"> | Symbol_<"A4"> | Symbol_<"B4"> | Symbol_<"_"> | { readonly $form: "n" } | { readonly $form: "rest" }>;
}

/** A phrase slot filled inline or by reference to a named phrase. */
export interface Audio_Track {
  $form: "track";
  $ns: "audio";
  lead: { readonly $form: "phrase" } | CrossRef<"phrase">;
}

/** A pitched note event. */
export interface Audio_N {
  $form: "n";
  $ns: "audio";
  $children?: unknown[];
}

/** A rest event. */
export interface Audio_Rest {
  $form: "rest";
  $ns: "audio";
  $children?: unknown[];
}

export type SjonAudio =
  | Audio_Phrase
  | Audio_Track
  | Audio_N
  | Audio_Rest;

