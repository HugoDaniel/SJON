// sjon-export-version: 1
// Generated by sjon — do not edit by hand.
// Describes the canonical JSON shape of `sjon to-json`.
//
// Export warnings:
//   [info head_set_emitted_via_oneof_refs] value-kind `pipeline-section` head-set emitted as `oneOf` of `$ref`s into `#/$defs/form.<plugin>.<head>`
//   [info head_set_emitted_via_oneof_refs] value-kind `bgl-resource` head-set emitted as `oneOf` of `$ref`s into `#/$defs/form.<plugin>.<head>`
//   [info head_set_emitted_via_oneof_refs] value-kind `data-shape-section` head-set emitted as `oneOf` of `$ref`s into `#/$defs/form.<plugin>.<head>`

export type Keyword<S extends string = string> = { readonly $kw: S };
export type Symbol_<S extends string = string> = { readonly $sym: S };
export type SjonDate = { readonly $date: string };
export type SjonTime = { readonly $time: string };
export type SjonExpr<TResult = unknown> = { readonly $expr: unknown[] } & { readonly __sjonResult?: TResult };
export type CrossRef<TargetForm extends string = string, S extends string = string> = Symbol_<S> & { readonly __sjonRef?: TargetForm };
export type F32 = number & { readonly __sjonRepr?: "f32" };
export type U32 = number & { readonly __sjonRepr?: "u32" };
export type I32 = number & { readonly __sjonRepr?: "i32" };
export type U16 = number & { readonly __sjonRepr?: "u16" };
export type F16 = number & { readonly __sjonRepr?: "f16" };


// ----- plugin: head-counts -----

/** The vertex stage of a pipeline. */
export interface HeadCounts_Vertex {
  $form: "vertex";
  $ns: "head-counts";
  entry: Symbol_;
}

/** The fragment stage. Optional, because a depth-only pass has none. */
export interface HeadCounts_Fragment {
  $form: "fragment";
  $ns: "head-counts";
  entry: Symbol_;
}

/** A pipeline-overridable constant. Any number of these. */
export interface HeadCounts_Constant {
  $form: "constant";
  $ns: "head-counts";
  name: Symbol_;
  value: number;
}

/** A pipeline whose sections are counted, not just typed. */
export interface HeadCounts_RenderPipeline {
  $form: "render-pipeline";
  $ns: "head-counts";
  name: Symbol_;
  /** Positional counts (not expressible in TS): vertex: 1; fragment: 0..1 */
  $children?: Array<{ readonly $form: "vertex" } | { readonly $form: "fragment" } | { readonly $form: "constant" }>;
}

/** Same kind, keyed slot: the counts ride along inertly here. */
export interface HeadCounts_SectionHolder {
  $form: "section-holder";
  $ns: "head-counts";
  section?: { readonly $form: "vertex" } | { readonly $form: "fragment" } | { readonly $form: "constant" };
}

/** Open form, same bounded slot: `:open true` does not silence the counts. */
export interface HeadCounts_ScratchPipeline {
  $form: "scratch-pipeline";
  $ns: "head-counts";
  /** Positional counts (not expressible in TS): vertex: 1; fragment: 0..1 */
  $children?: Array<{ readonly $form: "vertex" } | { readonly $form: "fragment" } | { readonly $form: "constant" }>;
  [key: string]: unknown;
}

/** A buffer binding. */
export interface HeadCounts_Buffer {
  $form: "buffer";
  $ns: "head-counts";
  type: Symbol_;
}

/** A sampler binding. */
export interface HeadCounts_Sampler {
  $form: "sampler";
  $ns: "head-counts";
  type: Symbol_;
}

/** A texture binding. */
export interface HeadCounts_Texture {
  $form: "texture";
  $ns: "head-counts";
  "sample-type": Symbol_;
}

/** Exactly one resource: the claim per-head bounds cannot make. */
export interface HeadCounts_BglEntry {
  $form: "bgl-entry";
  $ns: "head-counts";
  binding: number;
  /** Positional counts (not expressible in TS): buffer: 0..1; sampler: 0..1; texture: 0..1; any of the set: 1 */
  $children?: Array<{ readonly $form: "buffer" } | { readonly $form: "sampler" } | { readonly $form: "texture" }>;
}

export interface HeadCounts_Cube {
  $form: "cube";
  $ns: "head-counts";
  size: number;
}

export interface HeadCounts_Sphere {
  $form: "sphere";
  $ns: "head-counts";
  radius: number;
}

/** At most one generator: a ceiling-only set bound. */
export interface HeadCounts_Data {
  $form: "data";
  $ns: "head-counts";
  name: Symbol_;
  /** Positional counts (not expressible in TS): any of the set: 0..1 */
  $children?: Array<{ readonly $form: "cube" } | { readonly $form: "sphere" }>;
}

export type SjonHeadCounts =
  | HeadCounts_Vertex
  | HeadCounts_Fragment
  | HeadCounts_Constant
  | HeadCounts_RenderPipeline
  | HeadCounts_SectionHolder
  | HeadCounts_ScratchPipeline
  | HeadCounts_Buffer
  | HeadCounts_Sampler
  | HeadCounts_Texture
  | HeadCounts_BglEntry
  | HeadCounts_Cube
  | HeadCounts_Sphere
  | HeadCounts_Data;

